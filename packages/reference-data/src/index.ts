/**
 * @pacer/reference-data — University/AdmissionUnit/AdmissionRule/HistoricalOutcome
 * 시드·검수·버저닝 파이프라인 (admin-curated, §12). CompetitorSignal 자동수집 금지(§7.7.4).
 *
 * P0 내부 데모: `pnpm seed:p0`이 CSV를 검증한 뒤 Supabase/Postgres에 upsert한다.
 */
export const P0_REFERENCE_DATA_SOURCE = "internal-demo://sample-not-for-admission";
